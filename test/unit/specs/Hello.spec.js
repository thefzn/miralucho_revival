// import PaymentContainer from '@/PaymentContainer';

describe('PaymentContainer.vue', () => {
    it('should render correct contents', () => {
        // const Constructor = Vue.extend(PaymentContainer)
        // const vm = new Constructor().$mount()
        // expect(vm.$el.querySelector('.title-24').textContent)
        //     .to.equal('Your Payment Methods')
    });
});
